# Replace spaces in string with underscores

Replace spaces in string with underscores

## Usage

``` r
underscore(string)
```

## Arguments

- string:

  String to transform by replacing spaces with underscores.
